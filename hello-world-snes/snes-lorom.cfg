MEMORY {
    ROM:     start = $8000, size = $7FC0, file = %O, fill = yes, fillval = $00;
    HEADER:  start = $FFC0, size = $0020, file = %O, fill = yes, fillval = $00;
    VECTORS: start = $FFE0, size = $0020, file = %O, fill = yes, fillval = $00;
    BANK1:   start = $8000, size = $8000, file = %O, fill = yes, fillval = $00;
    BANK2:   start = $8000, size = $8000, file = %O, fill = yes, fillval = $00;
    BANK3:   start = $8000, size = $8000, file = %O, fill = yes, fillval = $00;
    BANK4:   start = $8000, size = $8000, file = %O, fill = yes, fillval = $00;
    BANK5:   start = $8000, size = $8000, file = %O, fill = yes, fillval = $00;
    BANK6:   start = $8000, size = $8000, file = %O, fill = yes, fillval = $00;
}

SEGMENTS {
    CODE:    load = ROM,     type = ro;
    HEADER:  load = HEADER,  type = ro;
    VECTORS: load = VECTORS, type = ro;
    IMG0A:   load = BANK1,   type = ro;
    IMG0B:   load = BANK2,   type = ro;
    IMG1A:   load = BANK3,   type = ro;
    IMG1B:   load = BANK4,   type = ro;
    IMG2A:   load = BANK5,   type = ro;
    IMG2B:   load = BANK6,   type = ro;
}
